The main.c C file can be compiled and executed in the command line with following steps:
- In the terminal, go to the directory the main.c, and "makefile" file are both situated in.
- Run the following command
	$ make

- Run the following command
	$ ./main

	After the program run you can test what ever command you want to test.
some of the command to test:
cat main.c
cat main.c &
history
history !n    ( where 0<n<10)
